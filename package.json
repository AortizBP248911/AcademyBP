{
  "name": "video-admin-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint",
    "email:dev": "pnpm --filter emails dev",
    "email:build": "pnpm --filter emails build"
  },
  "dependencies": {
  }
}
